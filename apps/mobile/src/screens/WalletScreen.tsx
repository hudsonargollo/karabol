import { Text, View } from 'react-native';

// TODO: fetch GET /wallet and render redeemable QR codes + badges (3.1, 3.5).
export function WalletScreen() {
  return (
    <View style={{ padding: 24 }}>
      <Text>Your rewards will appear here.</Text>
    </View>
  );
}
